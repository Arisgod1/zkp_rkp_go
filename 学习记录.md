# Java转go项目练习
## 跑通go服务+/register接口
### 初始化项目
```bash
go mod init github.com/Arisgod1/zkp_rkp_go
```
### 安装依赖
```bash
go get github.com/gin-gonic/gin
go get gorm.io/gorm
go get gorm.io/driver/postgres
go get github.com/spf13/viper
go get go.uber.org/zap
```
### 建目录
在 Windows 上开发 Go 项目，推荐安装 Git for Windows，使用 Git Bash 终端，这样可以直接使用 Linux 风格的命令，避免跨平台兼容性问题。
```git bash
mkdir -p cmd/server pkg/config pkg/logger internal/model internal/repository internal/service internal/controller
```

### 写main.go文件

先把基本接口写一下,假返回

#### 运行一下

```
go get github.com/gin-gonic/gin
go run ./cmd/server
```

### 加入真数据库交互

写了个数据库的结构体

然后把PostgreSQL启动

```bash
docker run --name zkp-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=zkp_auth -p 5432:5432 -d postgres:16
```

### 加入redis防重放

#### 依赖

```bash
go get github.com/redis/go-redis/v9
go get github.com/google/uuid
```

#### 运行redis

```bash
docker run --name zkp-redis -p 6379:6379 -d redis:8.4
```

#### 改代码

加入/challenge路由

记得引入是crypto/rand不是math/rand不然不安全的同时代码对不上

### 真验证的加入

先加了三个工具函数，分别用于从字符串读取大数值，计算c，大数计算然后取模

这里引入了c的真实值计算，记得改一下challenge接口的c计算，让redis存入正确的c，然后要记得验证失败也要删除挑战防止对一个挑战进行反复攻击

```go
if left.Cmp(right) != 0 {
    c.JSON(http.StatusUnauthorized, gin.H{"error": "zkp verification failed"})
    _ = rdb.Del(ctx, redisKey).Err()
    return
}
_ = rdb.Del(ctx, redisKey).Err()
```

### 将群参数真实引入

编写了dev的proof接口

要用数据库中不存在的用户名，不然公钥y测试时不一致

### 真正签发 JWT + 鉴权中间件返回真实Token

#### 安装jwt库

```bash
go get github.com/golang-jwt/jwt/v5
```

#### 配置jwt常量

```go
const jwtSecret = "replace-this-with-env-secret-in-prod"
const jwtExpireSeconds = 86400
```

#### 新增jwt工具方法

在parseToken中会有一个回调方法,这个用途可以说是项目有多个鉴权时可以采用不同的密钥,这个可以加入判断,但是本项目只是单个方式,所以显得多余

#### 新增中间件

就是在业务逻辑前加入一个鉴权的函数，用于拦截不合法的请求头以及jwt

```go
func authMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		...
	}
}
```

### 加入限流

#### 中间件函数

新增一个rateLimitMiddleware限流中间件函数。

然后利用Group将接口进行了一个归类

```go
auth := r.Group("/api/v1/auth")
```

### 分层封装

这节就学到了go的依赖注入怎么回事，创建更安全，以及了解方法值的含义，以及go中的Receiver关键语法

```go
func (a *AuthController) Register(c *gin.Context) {
    //     ↑ 这叫 receiver（接收者）
    // 表示：这个函数是给 AuthController 用的
    // 函数内部可以用 a.svc 访问结构体字段
}
```

然后鉴于AuthService是长生命周期的，context.Context是请求生命周期的,所以应该每个Service方法显式接收ctx参数,每次发请求都是新的ctx,防止过期。(GPT搞错了，kimi发现了漏洞)

```bash
E:.
├─.idea
├─cmd
│  └─server
├─internal
│  ├─auth
│  ├─controller
│  ├─model
│  ├─repository
│  └─service
└─pkg
    ├─config
    └─logger
```

#### jwt鉴权封装

新建鉴权中间件

新建用户控制器

main 里新增 user 路由组，之前已有的是auth组

### 统一错误码 + 日志追踪 + 请求ID（可观测性）

先是统一错误响应模型，类似Java的Result封装，然后

```go
func JSONError(c *gin.Context, status int, code string, msg string)
```

这个`code`的含义是标准错误码，类似`COMMON_INVALID_JSON`这种，区别于`status`是给HTTP看的，`code`是给业务逻辑看的。（后面的可能不打` `了,感觉累累的）

请求ID中间件，就是把每个请求设置一个requestId，让日志可追溯

### 限流中间件的加入以及yaml配置文件的应用

之前ai就教了，但是我忘了加

用的是viper进行配置的管理，字段会进行一个映射，代码：驼峰命名，YAML是蛇形命名，环境变量：大写下划线。

### 在分层解耦后的大boss------异步审计

当时写Springboot的异步审计没细看，ai帮助的(hhh)现在回过头，用go重写在细细的梳理逻辑。

#### 定义审计的事件模型

#### 定义发布接口（核心抽象）

#### 给一个Noop实现

然后就是在AuthService里面进行注入Publisher,目前是空实现,所以不影响实际逻辑

😱这里就非常有趣了，接入到AuthService中时，我发现这个用了一个defer，这个是go语言比较独特的一个函数前缀，在函数结束时运行，也就是说当前这个函数在方法中可以先定义好变量，然后运行过程中有什么问题，就把reason以及retErr等字段进行赋值，然后对应的审计函数的参数也就在运行的过程中被更新了，最后调用审计函数的实现进行实际情况的记录。

#### 加入kafka：激动人心

先加个依赖，不然会红^_^

```go
go get github.com/segmentio/kafka-go@latest
go mod tidy
```

先加配置项，温习一下配置，go代码中是驼峰，配置中是蛇形。

#### 新增 Kafka Publisher(同步)

#### 增加异步包装器(异步)

这个地方也很有意思，因为要保证每次审计的完整执行，所以kafka的publisher需要是同步的。

而程序在运行的过程中需要去异步开始审计，不能阻塞原本程序的运行情况所以需要异步包装器来进行包装。

细节上增加了kfkcfg的配置，开启审计则是kafka实现，不开启则是noop实现hhh。

```go
func (p *AsyncPublisher) Publish(ctx context.Context, e Event) error {
    select {
    case p.queue <- e:
       return nil
    default:
       // 队列满：丢弃并记日志（不影响主流程）
       log.Printf("[audit] queue full, drop event_type=%s event_id=%s", e.EventType, e.EventID)
       return nil
    }
}
```

select语法：原生支持多路复用，就是同时监听多个case，无 default 且无通道就绪 → 阻塞等待，有 default 且无通道就绪 → 就直接走default

sync.Once只执行一次

非常有趣，**Go 的哲学**：错误是值，不是控制流，有意思。

启动一下kafka

```go
docker run -d --name kafka \
  -p 9092:9092 \
  -e KAFKA_NODE_ID=1 \
  -e KAFKA_PROCESS_ROLES=broker,controller \
  -e KAFKA_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093 \
  -e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092 \
  -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
  -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT \
  -e KAFKA_CONTROLLER_QUORUM_VOTERS=1@localhost:9093 \
  -e KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 \
  -e KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1 \
  -e KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1 \
  -e KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS=0 \
  apache/kafka:4.0.0
```

然后不能黑盒开发，去看看应用往kafka发了什么。

```bash
docker exec -it kafka sh

/opt/kafka/bin/kafka-console-consumer.sh \
  --bootstrap-server localhost:9092 \
  --topic zkp_audit_events \
  --from-beginning
```

只看新消息，不看历史，把最后的 `--from-beginning` 去掉。

# Java转go项目练习
## 跑通go服务+/register接口
### 初始化项目
```bash
go mod init github.com/Arisgod1/zkp_rkp_go
```
### 安装依赖
```bash
go get github.com/gin-gonic/gin
go get gorm.io/gorm
go get gorm.io/driver/postgres
go get github.com/spf13/viper
go get go.uber.org/zap
```
### 建目录
在 Windows 上开发 Go 项目，推荐安装 Git for Windows，使用 Git Bash 终端，这样可以直接使用 Linux 风格的命令，避免跨平台兼容性问题。
```git bash
mkdir -p cmd/server pkg/config pkg/logger internal/model internal/repository internal/service internal/controller
```

### 写main.go文件

先把基本接口写一下,假返回

#### 运行一下

```
go get github.com/gin-gonic/gin
go run ./cmd/server
```

### 加入真数据库交互

写了个数据库的结构体

然后把PostgreSQL启动

```bash
docker run --name zkp-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=zkp_auth -p 5432:5432 -d postgres:16
```

### 加入redis防重放

#### 依赖

```bash
go get github.com/redis/go-redis/v9
go get github.com/google/uuid
```

#### 运行redis

```bash
docker run --name zkp-redis -p 6379:6379 -d redis:8.4
```

#### 改代码

加入/challenge路由

记得引入是crypto/rand不是math/rand不然不安全的同时代码对不上

### 真验证的加入

先加了三个工具函数，分别用于从字符串读取大数值，计算c，大数计算然后取模

这里引入了c的真实值计算，记得改一下challenge接口的c计算，让redis存入正确的c，然后要记得验证失败也要删除挑战防止对一个挑战进行反复攻击

```go
if left.Cmp(right) != 0 {
    c.JSON(http.StatusUnauthorized, gin.H{"error": "zkp verification failed"})
    _ = rdb.Del(ctx, redisKey).Err()
    return
}
_ = rdb.Del(ctx, redisKey).Err()
```
